

% programma principale %

var a ^= 4;\


func abbaa(c:integer,b:integer)->string:

    var f ^= 4;\
return 1;
endfunc




proc main():
    var operazione : string; \
    var input1, input2, answer : integer;\
    var flag ^= true;\
    var result : real;\

    if result >100 then
        var a ^=2;\
        flag ^= false;

    elseif result > 50 then
    		var valore ^= "media";\
     		size ^= valore;
    elseif result > 50 then
    		var valore ^= "media";\
     		size ^= valore;
    else
     var valore ^= "piccola";\

    endif;

endproc


